#!/usr/bin/env bash

binPath=~/.local/bin

sudo cp trackitClient.js $binPath/trackit
sudo chmod +x $binPath/trackit

# sudo cp trackitServer.js $binPath/trackitServer
# sudo chmod +x $binPath/trackitServer
